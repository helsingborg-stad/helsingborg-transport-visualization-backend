components:
  schemas:
    ILoginResponse:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
        token:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
